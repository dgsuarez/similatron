require 'spec_helper'

describe Similatron do
  it 'has a version number' do
    expect(Similatron::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
